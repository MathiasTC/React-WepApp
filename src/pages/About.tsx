import * as React from 'react';
import { CustomCard } from '../components/CustomCard';

export const About = () => {
    return (
        <div>
            <CustomCard whichPage={'about me'}/>
        </div>
    )
}