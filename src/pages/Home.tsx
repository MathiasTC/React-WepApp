import * as React from 'react';
import { CustomCard }from '../components/CustomCard';

export const Home = () => {
    return (        
        <div>
            <CustomCard whichPage={'home'}/>
        </div>
    )
}